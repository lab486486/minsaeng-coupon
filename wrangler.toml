name = "minsaeng-coupon"
compatibility_date = "2025-07-01"
pages_build_output_dir = "dist"

[[r2_buckets]]
binding = "MEDIA"
bucket_name = "minsaeng-coupon-media"
preview_bucket_name = "minsaeng-coupon-media"

# Client ID는 OAuth authorize URL에 노출되는 공개값 → 배포 시 Functions에 바인딩
[vars]
GITHUB_CLIENT_ID = "Ov23liNHMXLlTCY5gBS0"

# GITHUB_CLIENT_SECRET 은 Pages → Settings → Variables and Secrets (Secret)로만 설정
